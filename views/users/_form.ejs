
<script>
   function confirmPassword() {
      var pw1 = document.getElementById('user_password').value;
      var pw2 = document.getElementById('user_confirm_password').value;

      if (!pw1 || !pw2) {
         alert('Debe rellenar los campos de Password y Confirmación.');
         return false;
      }

      if (pw1 === pw2) {
         return true;
      } else {
         alert('Los passwords introducidos no coinciden.');
         return false;
      }
   }
</script>



<ul>

<li>
	<% if ( ! user.id) { %>
		<label for="preg" class="rem"><b>Nombre</b>:</label>
		<input type="text" id="username" name="user[username]" value="<%= user.username %>"/><p>
	<% } else { %>
		<b>Nombre</b>: <%= user.username %>
	<% } %>
</li>

<li>
	<label for="user_password" class="rem"><b>Password</b>:</label>
	<input type="password" id="user_password" name="user[password]"/> <p>
</li>

<li>
	<label for="user_confirm_password" class="rem"><b>Confirmar Password</b>:</label>
	<input type="password" id="user_confirm_password" /> <p>
</li>

</ul>
