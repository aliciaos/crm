

<h1> 
    Visitas: 

    <a href="/visits/new"><img width="24px" src="/images/add.png"></a>
</h1>

<table>
    <tr>
        <th>
            Cliente
        </th>
        <th>
            Vendedor
        </th>
        <th>
            Planificado
        </th>
        <th>
            Realizado
        </th>
        <th>
            Num.Obj.
        </th>
        <th>
            Observaciones
        </th>
        <th>
            
        </th>
    </tr> 

    <% for (var i in visits) { %> 
        <% var visit = visits[i]; %>      

        <tr>
            <td>
                <%= visit.Customer ? visit.Customer.code : "SIN CLIENTE" %>
            </td>
            <td>
                <%= visit.Salesman ? visit.Salesman.name : "SIN VENDEDOR" %>
            </td>
            <td>
                <%= moment(visit.plannedFor).format("DD-MM-YYYY") %>
            </td>
            <td>
            <%= !!visit.fulfilledAt ? moment(visit.fulfilledAt).format("DD-MM-YYYY") : "PENDIENTE" %>
            </td>
            <td>
                666
            </td>
            <td>
                <%= visit.notes === "" ? "No" : "Si" %>
            </td>
            <td>
                <a href="/visits/<%= visit.id %>"><img width="24px" src="/images/ver.png"></a>

                <a href="/visits/<%= visit.id %>/edit"><img width="24px" src="/images/edit.png"></a>

                <a href="/visits/<%= visit.id %>?_method=DELETE" 
                   onClick="return confirm('¿Seguro que quiere borrar la visita de <%= visit.Salesman.name %> a <%= visit.Customer.code %>?');"><img width="24px" src="/images/basura.png"></a>
            </td>
         </tr> 
    <% } %>

</table>

