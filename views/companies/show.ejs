
<h1> 
    Fábrica: 

    <a href="/goback"><img class="icon" src="/images/back.png"></a>

    <% if (session.user && session.user.isAdmin) { %>
        <a href="/companies/<%= company.id %>/edit"><img class="icon" src="/images/edit.png"></a>
    <% } %>
</h1>

<ul>
    <li>
        <b>Nombre</b>: <%= company.name %>
    </li>
</ul>


</h2>
Clientes Habituales:
</h2>

<ul>
    <% for (var i in mainCustomers) { %>
    <% var customer = mainCustomers[i]; %>

    <li> <%=  customer.code %> - <%=  customer.name %> </li>

    <% } %>
</ul>
