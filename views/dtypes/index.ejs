

<h1> 
    Diagnósticos: 

    <a href="/dtypes/new"><button type="button">Crear nuevo tipo</button></a>
</h1>

<ul>
  <% for (var i in dtypes) { %> 
    <% var dtype = dtypes[i]; %>      

     <li>
          <a href="/dtypes/<%= dtype.id %>"> <%= dtype.code %> - <%= dtype.title %> </a>

          <a href="/dtypes/<%= dtype.id %>/edit"><button>editar</button></a>

          <a href="/dtypes/<%= dtype.id %>?_method=DELETE" 
               onClick="return confirm('¿Seguro que quiere borrar el diagnóstico: <%= dtype.code %>?');">
               <button>borrar</button>
          </a>
     </li> 
  <% } %>
</ul>

